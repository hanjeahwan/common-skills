interface:
  display_name: "Codex–Luna Swarm"
  short_description: "Run Luna workers with Sol coordinator review"
  default_prompt: "Use $codex-luna-swarm for this coding task. Always try to form useful parallel work for GPT-5.6 Luna workers at max reasoning effort, then require the Sol coordinator to review the integrated result before delivery."
